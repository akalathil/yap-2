<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <meta name="description" content="">
    <meta name="author" content="">

    <link href="../css/yap.css" rel="stylesheet">
    <link href="../css/bootstrap.min.css" rel="stylesheet">
    <link href="../font-awesome/css/font-awesome.min.css" rel="stylesheet" type="text/css">

</head>

<body>

    <!-- listing -->
    <div id="wrapper">
    <div class="content" id="page-content-wrapper">
        <div class="section group" id="teach-listing">
            <h2>Search</h2>
            <br>
            <form class="search-form" action="/teach_listing_search" method="get">
            <div class="row" id="input-row">
                <input type="text" name="search" id="search-text"></input>
                <a href="#" id="search">
                    <i class="fa fa-search fa-lg"></i>
                </a>
            </div>
            </form>
        </div>
        <h4>{{empty_message}}</h4>
        {{#each result}}
        <div class="list-box col-xs-12">
            <a href="/user_profile?name={{name}}">
                <div class="box">
                    <div class="inner row">
                        <div class="profile col-xs-4">
                            <img class="img-circle" src={{img-src}}>
                        </div>
                        <div class="col-xs-8" id="name-rating">
                            <div class="row" id="name">
                                <h3>{{name}}</h3>
                            </div>
                            <div class="row" id="rating">
                                    <div class="col-xs-7">
                                        <span>{{rating}}/5.0</span>
                                    </div>
                                    <div class="col-xs-5">
                                        <span id="dollar-sign">{{pricing}}</span>
                                    </div>
                            </div>
                        </div>
                    </div>
                    <div class="row">
                        <div class="skill-box col-xs-4">{{skill_1}}</div>
                        <div class="skill-box col-xs-4">{{skill_2}}</div>
                    </div>
                </div>
            </a>
        </div>
        {{/each}}
    </div>
    <!-- side-bar -->
    {{> side-bar}}
</div>
    <!-- footer -->
    {{> footer}}
    {{> googleAnalytics}}

</body>
<style>
    h3{
        font-size: 27px;
    }
    h4 {
        text-align: center;
    }
    #input-row {
        text-align: center;
    }
    #teach-listing {
        text-align: center;
    }
    #search-text {
        font-size: 1.3em;
        border-radius: 3px;
    }
    .list-box {
        margin: 10px auto;
        height: 156px;
    }
    .inner {
        margin: 0px;
    }
    .icon-label {
        font-size: 15px;
    }
    .header-icon {
        font-size: 1.15em;
    }
    #page-content-wrapper {
        padding-bottom: 75px;
    }
    .profile {
        padding: 7px;
    }
    .img-circle {
        width: 100px;
        height: 100px;
        border: 2px solid black;
        border-radius: 50%;
    }
    .skill-box {
        font-size: 16px;
        display: inline-block;
        border: 1px solid ##55B4B0;
        border-radius: 5px;
        text-align: center;
        margin: 0px 5px;
        left: 20px;
        top: 5px;
        right: 20px;
        background-color: #55B4B0;
        color: white;
    }
    .content, #teach-listing, h2{
        background-color: #00b2ca;
        font-weight: bold;
    }
    body {
        background-color: #00b2ca;
    }
</style>

</html>
